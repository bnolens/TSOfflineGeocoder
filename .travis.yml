# reference: http://www.objc.io/issue-6/travis-ci.html

language: objective-c
# before_install: cd Example && pod install && cd -

script:

    - pod lib lint
		- xctool -workspace Example/TSOfflineGeocoder.xcworkspace -scheme TSOfflineGeocoder -configuration Release -sdk iphonesimulator -arch i386 build